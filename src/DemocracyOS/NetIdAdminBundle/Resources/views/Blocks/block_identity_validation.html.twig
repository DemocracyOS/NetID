{% if admin.isGranted('VALIDATE') %}
<table class="table table-bordered table-striped sonata-ba-list">
    <thead>
        <tr>
            <th colspan="3">{{ "Identity Validation" | trans }}</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td>
                <div class="btn-group" align="center">
                    <a class="btn btn-small" href="{{ admin.generateUrl('identityValidateSearch') }}">{{ "Validate Identity" | trans }}</a>
                </div>
            </td>
        </tr>
    </tbody>
</table>
{% endif %}